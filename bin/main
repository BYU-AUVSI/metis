#! /usr/bin/env python

import rospy
from metis.ros.mission import MissionPlanner

if __name__ == '__main__':
    rospy.init_node('main_planner', anonymous=True) # log_level=rospy.DEBUG
    test_planner = MissionPlanner()

    while not rospy.is_shutdown():
        rospy.spin()
